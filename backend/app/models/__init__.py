"""SQLAlchemy models.

Importing this package must register every table on `Base.metadata`, so Alembic
autogenerate can see them. Add each new model module to the imports below.

Empty for now — tables arrive with the features that read them, per the design in
.kiro/specs/01-profile-and-resume/design.md. A migration creating tables nothing
queries is a guess awaiting correction.
"""
